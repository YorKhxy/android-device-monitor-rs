# 日志模块重构方案 —— 一比一对齐 Android Studio Logcat

> 目标:抓得全、连上即见历史、过滤像 AS 一样强,且高频设备(Pico)不卡。
> 现状已对齐 AS 很多,本方案只补差距,不推倒重来。

---

## 0. 先回答那个关键反问:AS 高频洪流为什么不卡死?

AS 不卡,**靠的不是"少抓",而是这套组合拳**:

1. **抓取在后台线程**,永不阻塞 UI。
2. **有界内存模型**(cyclic buffer,可配置,默认很大)——超了丢最旧。
3. **虚拟化渲染**——只画视口内那几十行,缓冲里有 10 万行也不影响绘制成本。
4. **批量合并刷新**——攒一批再更新文档,不逐条刷。
5. **过滤跑在内存模型上**,增量算。

**而你们的工具,这五条已经全有了**:
- `ChunkedLogStore`(环形缓冲,默认 2 万条上限,超了丢最旧)= 第 2 条
- 自定义变高虚拟滚动(`logRowOffsets` 前缀和 + overscan)= 第 3 条
- `bumpLogVersionThrottled`(无过滤 300ms / 过滤态 60ms 节流)= 第 4 条
- `filteredLogs` 增量过滤 + 缓存 = 第 5 条
- 后端 `BATCH_MAX=200 / FLUSH_MS=120` 批量 emit = 第 1+4 条

**所以"只拉最近 2000 行"那个 cap,如今已经不是防卡的命根子了——它是当年的过度保守遗留。** 渲染早就是 O(视口) 不是 O(总量)。我们完全可以**像 AS 一样连上就回放整个缓冲**,只要:
- 把内存上限调大并可配(AS 默认就大);
- 初始那坨历史**分批喂**进来(别一次性一个巨型 IPC 包砸过来),现有批处理管线天然就这么干;
- 高频洪流由现有"环形丢最旧 + 虚拟化 + 节流"吸收,**和 AS 同一个原理**。

一句话:**学 AS 的"全量进、有界存、虚拟化画",而不是学不到只能"少抓"。**

---

## 1. 现状 vs AS 差距(确认要补的)

| 维度 | AS | 现状 | 本方案 |
|------|-----|------|--------|
| 缓冲区 `-b` | `main,system,crash` 多缓冲 | 不指定(只默认) → **会漏 crash/system** | ✅ Phase1 |
| 连上历史 | 回放整个 ring buffer | 只拉最近 2000 行 | ✅ Phase1 |
| 内存上限 | 大且可配 | 固定 2 万 | ✅ Phase1 调大可配 |
| 过滤 | 查询语言 `tag: package: level: -neg ~正则 \| 或` | 分散的独立输入框 | ✅ Phase2 |
| `package:mine` | 只看可调试自家应用 | 无 | ✅ Phase2(近似) |
| 进程启停标记 | PROCESS STARTED/ENDED | 仅 ps 轮询补包名 | ⬜ Phase3 可选 |
| 时间戳 | epoch/带年 | `-v long` 不带年补当前年 | ⬜ Phase3 可选 |

---

## 2. Phase 1 —— 抓全 + 连上即见全量历史(后端为主)

**改 `src-tauri/src/adb/logcat_stream.rs`:**

1. **加多缓冲区**:抓取命令加 `-b main,system,crash`(best-effort,某些设备不支持就退回默认)。直接解决"抓不全"——crash buffer 里的崩溃、system buffer 里的系统服务日志现在都进得来。
2. **连上回放整缓冲(对齐 AS)**:`include_history=true` 时**去掉 `-T 2000` 上限**,改成不带 `-T`(或 `-T 1` 起点)→ logcat 先回放当前缓冲全部、再续接实时,正是 AS 的单流行为。`include_history=false` 仍 `-T <设备当前时刻>` 只收新日志。
3. **删 `HISTORY_TAIL_LINES=2000`** 常量及相关分支。
4. **`-G 16M` 保留**(已有,给足历史留存)。

**改 `src/renderer/lib/logStore.ts`:**
5. `MAX_LOG_ENTRIES` 2 万 → **5 万**(或做成设置项,AS 默认就大)。虚拟化在,5 万行渲染无压力。

**洪流兜底验证(Pico)**:全量回放时初始那坨大,但走的还是 `BATCH_MAX/FLUSH_MS` 批量 + 前端环形丢最旧 + 虚拟化 + 节流。**理论上吸收得住**——这条要真机 Pico 实测确认不卡(四步走里的功能测试)。

---

## 3. Phase 2 —— AS 过滤查询语言(前端为主)

**核心:把现在分散的"等级下拉/tag 框/包名框/pid 框/搜索框"统一成一个 AS 式查询条**(独立字段保留为"快捷 chip",点一下编译成查询 token,兼顾老习惯)。

**新增 `src/renderer/lib/logcatFilter.ts`** —— 查询语言解析器 + 谓词编译:

支持的语法(对齐 AS):
- **键值**:`tag:Foo` `package:com.x` `process:1234`(pid 或进程名) `level:W`(≥该级) `message:hello` `line:xxx`(任意字段)
- **裸文本**:`hello` = 在 message/tag/包名/pid 里子串匹配(= 现在的搜索框行为)
- **逻辑**:空格 = 与;`|` = 或;`-` 前缀 = 非(`-tag:System` `-spam`)
- **匹配修饰**:`~` 正则(`tag:~Activity.*`);`=` 精确(`tag:=Foo`);默认大小写不敏感子串
- **特殊**:`package:mine` = 只看自家应用。**近似实现**:`pm list packages -3`(第三方应用)缓存,命中包名集即算 mine;非完美等同 AS 的 debuggable 标志,但实用够用(会在 UI 注明)。

**改 `SimpleApp.tsx`:**
- `filteredLogs` 的 `test()` 改成调用编译后的谓词(替换现有 if 链)。
- 过滤条 UI:主查询输入框 + 现有快捷 chip;`package:mine` 需要拉一次 `pm list packages -3`(新增一个后端命令或复用 `list_installed_packages`)。

**新增后端命令**(若需要):`list_debuggable_packages`(`pm list packages -3`),供 `package:mine` 用。

---

## 4. Phase 3 —— 可选增强(确认要不要)

- **PROCESS STARTED / ENDED 标记**:从 ps 轮询的 diff 或 `ActivityManager` 的 `am_proc_start/died` 注入合成标记条,像 AS 那样显示进程启停分隔。
- **时间戳带年**:`-v long` → `-v year` 或 epoch,解析器同步改。(目前补当前年,跨年才有坑,优先级低。)

---

## 5. 风险 & 取舍

1. **全量回放在 Pico 上的首屏延迟**:几十万行历史一次性回放,首屏会有 1~3 秒"灌入"。可接受(AS 也有),且只在"包含历史"开关开时。要更稳可加"回放上限可配"(默认很大,洪流设备可调小)——这其实就是 Q2 里那个"智能降级",我倾向**默认全量 + 留一个可配上限兜底**。
2. **`-b all` 太吵**:故只取 `main,system,crash`(对齐 AS 默认),不取 events/radio/kernel。
3. **`package:mine` 不完美**:第三方应用近似,文档注明。
4. **过滤条 UI 改动**:统一查询条是体验变化,保留快捷 chip 降低迁移成本。

---

## 6. 落地顺序

- **Phase 1** 先做(抓全 + 历史):改动小、收益最直接,真机 Pico 验洪流。
- **Phase 2** 再做(查询语言):前端为主,工作量最大。
- **Phase 3** 看你要不要。

> 确认这个方案(或圈掉不要的部分),我就按 Phase 1 → 2 开干。
