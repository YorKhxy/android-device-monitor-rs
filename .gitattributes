# Windows batch files must use CRLF. With LF, cmd.exe miscomputes file
# offsets (assumes CRLF) and eats line-leading bytes each line
# (e.g. "chcp 65001" runs as "5001"). Lock CRLF on checkout.
*.bat text eol=crlf
*.cmd text eol=crlf

# Everything else: normalize to LF in repo.
* text=auto eol=lf
